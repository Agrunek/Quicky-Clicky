import Link from '@/components/Link';

const FloatingBackButton = () => {
  return (
    <Link to=".." className="fixed top-4 left-8 min-w-0!">
      Home page
    </Link>
  );
};

export default FloatingBackButton;
